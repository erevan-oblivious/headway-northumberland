---
import '../styles/global.css';
import { Image } from 'astro:assets';
import LogoImg from '../images/headway-logo.png';

interface Props {
  title?: string;
  description?: string;
}

const { 
  title = "Headway Northumberland", 
  description = "Supporting brain injury survivors across Northumberland." 
} = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/headway-logo.svg" />
    <title>{title}</title>
  </head>
  <body>
<header class="header">
  <div class="header-inner">
      <a href="/" class="logo-link">
        <Image src={LogoImg} alt="" class="header-logo" loading="eager"/>
        <span class="logo-title">Headway Northumberland</span>
      </a>

    <!-- Hamburger Toggle Button -->
    <button class="hamburger" id="hamburger-btn" aria-label="Toggle navigation menu" aria-expanded="false">
      <span class="bar"></span>
      <span class="bar"></span>
      <span class="bar"></span>
    </button>

    <!-- Navigation Menu -->
    <nav class="nav-links" id="nav-menu">
      <a href="/">Home</a>
      <a href="/news">News</a>
      <a href="/events">Events</a>
      <a href="/contact">Get In Touch</a>
    </nav>
  </div>

</header>

<script>
  const hamburgerBtn = document.getElementById('hamburger-btn');
  const navMenu = document.getElementById('nav-menu');

  if (hamburgerBtn && navMenu) {
    hamburgerBtn.addEventListener('click', () => {
      const isOpen = navMenu.classList.toggle('is-active');
      hamburgerBtn.classList.toggle('is-active');
      hamburgerBtn.setAttribute('aria-expanded', isOpen.toString());
    });
  }
</script>

<main class="main">
  <slot />
</main>
<footer class="footer">
<hr class="divider-full">
  <p>Headway Northumberland is registered with the Charity Commission for England (Charity no. 1198408).</p>
  <p>&copy; {new Date().getFullYear()} Headway Northumberland. All rights reserved.</p>
  <p><a href="/headway-privacy-policy.pdf" target="_blank" rel="noopener noreferrer" class="hover:underline">Privacy Policy</a></p>
  <p><a href="/cookies">Cookie Policy</a></p>
  <p><a href="/accessibility">Accessibility Statement</a></p>
</footer>
  </body>
</html>